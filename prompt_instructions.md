# 📊 Vercel 안전 배포용 Gemini Canvas 프롬프트 지침 (매월 1일 업데이트 가이드)

이 문서는 매월 1일, 한 달간의 소비 데이터를 기반으로 **Gemini Canvas**를 통해 새로운 대시보드 코드를 생성할 때 사용할 **최적화된 프롬프트 지침**입니다.

이 지침을 복사해서 Gemini Canvas에 입력하면, 기존의 **Vercel 백엔드 보안 구조(/api/analyze 호출)** 및 **토스 스타일의 360도 도넛 차트 및 라벨 겹침 방지 알고리즘**을 완벽하게 유지하면서 데이터만 새롭게 업데이트된 완벽한 코드를 얻을 수 있습니다.

---

## 🔒 백엔드(/api/analyze) 호출 구조가 필수적인 이유
* **API 키 보안**: HTML 브라우저 단독(클라이언트 사이드)으로는 Vercel의 환경 변수(`process.env.GEMINI_API_KEY`)를 직접 읽을 수 없습니다. 만약 HTML 코드 내에 API 키를 하드코딩하게 되면 외부인에게 키가 그대로 노출되어 도용당할 위험이 있습니다.
* **서버리스 작동 방식**: 이를 해결하기 위해 Vercel의 `api/analyze.js` 백엔드 서버가 환경변수를 안전하게 읽고 Gemini API를 대리 호출합니다. HTML은 브라우저에서 `/api/analyze` 경로로 요청을 보내 결과만 받아옵니다.
* **따라서 새로 생성되는 HTML 역시 반드시 `/api/analyze`를 호출하는 비동기 fetch 구조를 완벽하게 유지해야 합니다.**

---

## 📋 매월 1일 Gemini Canvas 입력용 최적화 프롬프트 (그대로 복사하여 사용)

> [!TIP]
> 아래의 박스 내용을 그대로 복사(Ctrl+C)하여 매월 1일 Gemini Canvas 대화창에 넣고, 맨 아랫부분에 **새로운 6월 소비 데이터**를 붙여넣어 생성하시면 됩니다.

```text
아래의 규칙과 지침을 완벽하게 준수하여 새로운 한 달간의 소비 분석 및 AI 리포트 HTML 코드를 완성해 줘.

### 🚨 [가장 중요] Vercel 백엔드 연동 및 보안 규칙
1. 브라우저에서 Gemini API 키를 직접 호출하거나 하드코딩하지 마십시오.
2. AI 분석 리포트 버튼을 누르면 반드시 Vercel 서버리스 백엔드 엔드포인트인 "/api/analyze"로 POST 요청을 보내도록 작성하십시오.
3. 요청 본문(Body)에는 JSON 형태로 { totalAmount, data }를 실어서 보내야 하며, 호출 코드는 반드시 다음과 같은 비동기 호출 및 폴백(Fallback) 구조를 그대로 유지해야 합니다:

   const result = await fetch("/api/analyze", {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify({ totalAmount, data })
   });

4. 만약 백엔드 호출이 실패하거나 환경변수가 없을 경우에 대비해, 에러 캐치(catch) 블록 내에 UX를 보장하는 "고품질 Mock 소비 분석 리포트" 텍스트가 노출되는 예외 처리(Fallback) 로직을 완벽하게 유지해 줘.

### 🎨 프리미엄 UI 및 도구 규칙 (토스 뱅크 스타일)
1. 전체 레이아웃은 토스 뱅크 스타일의 다크 테마(#121212), 매끄러운 글래스모피즘 효과, 부드러운 호버 애니메이션, 그리고 아름다운 구글 Inter/Outfit 폰트를 유지해 줘.
2. 360도 도넛 차트의 라벨선이 서로 겹치거나 글자가 깨지지 않도록, 각 카테고리의 각도를 계산하여 8방향 방사형으로 밀어내고 수평 Elbow 지시선을 긋는 물리적 이격(Spring) 충돌 방지 배치 알고리즘을 기존 코드 구조 그대로 유지하여 적용해 줘.
3. SVG 도넛 차트의 중앙에 위치하는 텍스트와 주변 지시선이 왜곡 없이 아름답게 표현되어야 해.

### 📊 업데이트할 새로운 데이터
여기에 새로 업데이트할 소비 데이터를 제공할게. 이 데이터를 기반으로 'const RAW_DATA = [...]' 배열 부분을 완전히 교체해 주고, 카테고리별 합계와 백분율(percent)을 정확하게 다시 계산해서 적용해 줘.

[여기에 6월 한 달간의 텍스트 데이터 혹은 카드 결제 내역을 붙여넣으세요!]
```

---

## 🛠️ 추천하는 가장 쉬운 업데이트 방법 2가지

### 방법 A: 기존 파일 첨부하기 (권장)
1. 7월 1일에 Gemini Canvas를 켤 때, 이 저장소의 기존 `index.html` 파일을 대화창에 파일 첨부(업로드)합니다.
2. 위 **복사용 프롬프트**와 함께 **6월 소비 내역**을 함께 전달합니다.
3. Gemini가 완성해 준 전체 코드를 그대로 복사하여 `index.html`에 덮어쓰고 깃허브에 푸시(Commit & Push)합니다.

### 방법 B: `RAW_DATA` 부분만 수동 교체하기
1. Gemini Canvas에 새 소비 데이터만 주어 일반적인 대시보드 코드를 생성하도록 합니다.
2. 새로 만들어진 코드에서 `const RAW_DATA = [ ... ]` 부분만 마우스로 긁어 복사합니다.
3. 기존의 정상 작동하는 `index.html`을 열고 `const RAW_DATA` 부분만 새로 복사한 코드로 쏙 갈아 끼워 줍니다.
4. 이 방식은 백엔드 통신 코드(`/api/analyze`)나 디자인 코드가 절대 깨지지 않는 가장 안전하고 빠른 방법입니다.
