# 🕊️ [SKILL] 영상 데이터 전수 조사 및 토스형 Canvas 실시간 구현 (Vercel API 보안 연동 통합 지침)

이 문서는 매월 1일, 한 달간의 소비 데이터를 기반으로 **Gemini Canvas**를 통해 새로운 대시보드 코드를 생성할 때 입력할 **최적의 프롬프트 통합 지침**입니다. 기존의 프리미엄 UI 및 360도 도넛 차트 지침에 **Vercel API 보안 연동 및 서버리스 백엔드 호출 구조**를 유기적으로 병합하여 완성하였습니다.

---

## 📋 매월 1일 Gemini Canvas 입력용 통합 프롬프트 지침 (그대로 복사하여 사용)

> [!TIP]
> 아래의 박스 내용을 그대로 복사(Ctrl+C)하여 매월 1일 Gemini Canvas 대화창에 넣고, 맨 아랫부분의 데이터 입력 칸에 **새로운 6월 소비 데이터**를 붙여넣어 생성하시면 됩니다.

```text
아래 기술된 5가지 규칙과 지침을 100% 준수하여 백엔드 보안 연동이 완벽하게 반영된 프리미엄 소비 분석 및 AI 리포트 단일 HTML 코드를 완성해 줘.

---

### 1. 데이터 분석 및 추출 (Back-end Logic & OCR)
* **1:1 전수 추출**: 업로드된 영상(또는 이미지)의 모든 프레임을 교차 검증하여 소비 내역을 누락 없이 100% 추출한다. '외 n건' 식의 압축이나 임의 생략은 절대 금지한다.
* **상태 기반 필터링 및 합산 예외 처리 (가장 중요)**:
  - **인정(활성화)**: 글자색이 밝고 선명한(White/Light) 항목만 실제 지출 데이터로 추출하고 총액에 합산한다.
  - **무시(비활성화)**: 페이드아웃(흐린 회색) 처리된 취소 내역이나 할부 내역은 데이터 리스트에는 보여주되 시각적으로 음영/취소선 처리하고, 각 카테고리 총액 및 전체 총 지출 합계에서는 완벽하게 제외(소거)한다. (합산된 총액은 녹화된 영상의 총액과 반드시 일치해야 함)
* **총합 검증 및 절대 기준 (Source of Truth)**:
  - 각 카테고리별 상세 화면 상단에 노출된 '카테고리별 총 금액'을 절대적인 진실로 취급한다.
  - 전체 총 지출 합계 = 활성화된 각 카테고리별 총액의 합

---

### 2. 시각적 구현 및 UI/UX (Front-end Design)
* **UI 컨셉**: 토스(Toss) 스타일의 다크모드 기반 세로형 모바일 대시보드(Tailwind CSS 활용)를 구현한다.
* **상단 요약 (헤더)**: 메인 화면 최상단에 해당 월을 표시하고, '총 지출 합산 금액'을 가장 크고 역동적으로 배치한다.
* **도넛 차트 클릭 유도 배너**: 총 지출액 바로 아래 화면 중앙에 '카테고리 비중 확인'을 유도하는 대형 배너 카드를 배치한다. (은은한 후광(Pulse Glow - animate-pulse-glow), 알림 핑(Ping), 바운싱 아이콘 등 시각적 CTA 장치 필수)
* **브랜드 로직 (Auto-Branding)**: 결제처 이름을 분석하여 자동으로 브랜드 아이덴티티를 맵핑한다.
  - [인지된 브랜드]: DB손해보험(흰색/녹색), 배달의민족(민트/흰색), 무신사(블랙/흰색), 스타벅스, 당근, 컬리, CU, GS25, 던킨도너츠, 국민연금/건강 등 고유 색상과 이모지 또는 브랜디드 아이콘을 동적 생성한다.
  - [기타 상점]: 카테고리 성격에 맞는 FontAwesome 아이콘을 적용한다.
* **인터랙션**: React State를 활용하여 리스트와 상세 내역 뷰를 오갈 때 토스 앱 특유의 좌우 슬라이드(Slide In/Out) 애니메이션을 부드럽게 구현한다.

---

### 3. 대형 도넛 차트 시각화 제약 (Data Visualization)
* **중앙 텍스트 분리형 UI**: 차트 모달 렌더링 시, '총 지출 금액'은 도넛 중앙에 구겨 넣지 않고 모달 상단의 독립된 요약 배너로 분리하여 깔끔하게 배치한다.
* **8방향 방사형(Radial) 분산 배치**: 지시선과 데이터가 좌/우 한 곳에 조잡하게 몰리지 않도록, 스프링/물리 엔진 기반 로직을 사용하여 동/서/남/북 및 대각선(360도) 방향으로 고르고 넓게 퍼져 나가도록 구현한다.
* **관통 및 겹침 절대 금지 (Strict Collision)**:
  - 지시선(Polyline)은 반드시 도넛의 '바깥쪽 가장자리'에서 출발하여, 도넛 중앙 구멍을 관통하는 시각적 오류를 원천 차단한다.
  - 카테고리 이름, 퍼센트, 금액을 나타내는 텍스트 라벨과 선들이 절대 서로 겹치지(Overlap) 않아야 하며, 화면 밖으로 잘리지 않도록 SVG ViewBox 여백을 충분히 확보한다.

---

### 4. AI 소비 분석 기능 (Vercel 백엔드 API 연동 보안)
* 화면 우측 하단에 항상 떠 있는 플로팅 버튼(FAB)을 구현한다.
* **보안 준수**: API Key 노출 및 도용 방지를 위해 **브라우저 직접 호출이나 API Key 하드코딩은 절대 금지**합니다.
* **백엔드 호출 구조**: 버튼 클릭 시 Vercel 서버리스 백엔드 엔드포인트인 `/api/analyze`로 POST 요청을 보내어 실시간으로 분석 리포트를 생성해야 합니다.
  - 요청 본문(Body)에는 JSON 형태로 `{ totalAmount, data }`를 전달합니다.
  - 비동기 호출 및 재시도(fetchWithRetry) 패턴을 적용하여 통신의 안정성을 극대화합니다.
* **안전 폴백(Fallback) 예외 처리**:
  - Vercel 환경변수 설정 전이거나 API 에러 발생 시 사용자 이탈 및 에러 노출을 원천 차단하기 위해, 로딩 스켈레톤 작동 후 즉시 **고품질 Mock 소비 분석 리포트**를 모달에 노출시켜 UX 유연성을 확보하십시오.

---

### 5. 최종 출력 제약 (Output Constraint)
* **Strict Output**: 인사말, 로직 설명, 추가적인 확인 질문 등 기타 텍스트는 일절 출력하지 않는다.
* 위 로직이 모두 반영된 **단일 HTML 파일(React + Babel + Tailwind CDN 포함)** 코드 블록만을 즉시 출력하여, 사용자가 즉각적으로 Canvas(Web UI)를 확인할 수 있게 한다.

---

### 📊 업데이트할 새로운 데이터
여기에 새로 업데이트할 소비 데이터를 제공할게. 이 데이터를 기반으로 'const RAW_DATA = [...]' 배열 부분을 완전히 교체해 주고, 카테고리별 합계와 백분율(percent)을 정확하게 다시 계산해서 적용해 줘.

[여기에 6월 한 달간의 텍스트 데이터 혹은 카드 결제 내역을 붙여넣으세요!]
```
